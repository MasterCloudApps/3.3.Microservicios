server.port=8100
monolith.url=http://${HOST:localhost}:8080/

# spring traces
logging.level.org.springframework.web=DEBUG

# open-api
springdoc.api-docs.path=/api-docs

# swagger
springdoc.swagger-ui.operationsSorter=alpha

employee.url=http://${HOST:localhost}:8080/
